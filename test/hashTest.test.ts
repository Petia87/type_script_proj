/*import { Hash } from "../src/hash"
import { assert } from "chai"


describe("Hash", function () {
    let ht:any;
    beforeEach(() => {
        ht = new Hash()
    })
  
  
    it('set', function () {
        ht.set("petia", 10);
        let result = ht.get("petia");
        assert.equal(result, 10)

    })

    it('get', function () {
        ht.set("ani", 1);
        ht.set("ina", 1);
        let result = ht.get("ani");
        assert.equal(result, 1)
    })

    it('remove', function () {
        ht.set("ani", 1);
        let result = ht.remove("ani", 1);
        //assert.equal(result, undefined)
    })

})*/